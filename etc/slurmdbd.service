[Unit]
Description=Slurm DBD accounting daemon
After=network.target
ConditionPathExists=${prefix}/etc/slurmdbd.conf

[Service]
Type=forking
ExecStart=${exec_prefix}/sbin/slurmdbd
PIDFile=/var/run/slurmdbd.pid

[Install]
WantedBy=multi-user.target
